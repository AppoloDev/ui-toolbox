{% apply spaceless %}
    <div class="timeline-middle {{ this.getClasses }}" {{- attributes -}}>
        {%- if block('content') is defined -%}
            {{ block('content') }}
        {%- endif -%}
    </div>
{% endapply %}
