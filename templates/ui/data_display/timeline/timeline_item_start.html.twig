{% apply spaceless %}
    <div class="timeline-start {{ this.getClasses }}" {{- attributes -}}>
        {%- if block('content') is defined -%}
            {{ block('content') }}
        {%- endif -%}
    </div>
{% endapply %}
