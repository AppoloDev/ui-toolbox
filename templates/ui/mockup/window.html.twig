{% apply spaceless %}
    <div class="{{ this.getClasses }}" {{- attributes -}}>
        {%- if block('content') is defined -%}
            {{ block('content') }}
        {%- endif -%}
    </div>
{% endapply %}
